#Add all the natural numbers below one thousand that are multiples of 3 or 5.

total = 0
for n in range(1000):
   if n%3 == 0:
       total = n + total
   elif n%5 == 0:
       total = n + total

print(total) 
